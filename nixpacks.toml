# Nixpacks configuration for Railway
# This tells Railway to use Java 17 and Maven for the backend

[phases.setup]
nixPkgs = ['jdk17', 'maven']

[phases.build]
cmds = ['cd backend && mvn clean package -DskipTests']

[phases.start]
cmd = 'cd backend && java -jar target/natural-flow-1.0.0.jar'
